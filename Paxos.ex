def module Paxos do
    def start (name, participants) do
    
    
    end

    def propose (pid, inst, value, t) do
        state = receive do
            (:decision, v) ->

            (:abort) ->

            (:timeout) ->

            
        end
    end

    def get_decision(pid, inst, t) do

    end
        
end